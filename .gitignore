# Node.js dependencies — installed locally per-project, never committed
node_modules/

# npm lock file (keep package-lock.json, ignore this)
# package-lock.json  ← do NOT ignore this, it should be committed

# OS files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
